#!/usr/bin/env bash
# QtScrcpy Phone Farm Manager - Simple & Fast Development Environment

# Use nix-direnv for better caching and performance
if ! has nix_direnv_version || ! nix_direnv_version 3.0.4; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.0.4/direnvrc" "sha256-DzlYZ33mWF/Gs8DDeyjr8mnVmQGx7ASYqA5WlxwvBG4="
fi

# Load the default development environment (includes everything you need)
use flake . --impure

# Add wrapper scripts to PATH (they're in .direnv/bin which is auto-added by direnv)
# Wrapper scripts call functions from scripts/farm-functions.sh

echo ""
echo "✅ Farm Manager ready!"
echo "🚀 Quick start: build_release && run_qtscrcpy_farm"
echo ""

# Performance optimization: suppress verbose direnv output
export DIRENV_LOG_FORMAT=